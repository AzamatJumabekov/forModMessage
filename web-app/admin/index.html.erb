<h1>Templates index</h1>

<table class="table table-striped">
  <tr>
    <th>Template name</th>
    <th>Actions</th>
  </tr>
  <% @templates.each do |template| %>
  <tr>
    <td> <%= template %> </td>
    <td>
      <a href="/admin/delete/<%= template %>" class="glyphicon glyphicon-remove" onclick="return confirm('Are you sure?')"></a>
      <a href="/admin/show/<%= template %>" class="glyphicon glyphicon-search")></a>
      <a href="/admin/edit/<%= template %>" class="glyphicon glyphicon-pencil"></a>
    </td>
  </tr>
  
<% end %>
</table>